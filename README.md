<div align="center">
  <img src="assets/logo-128.png" width="96" height="96" alt="文章摆渡 Logo">
  <h1>⛵ 文章摆渡 · Article Ferry</h1>
  <p><b>在掘金写作，一键摆渡，安全着陆至 CSDN 与微信公众号草稿箱。</b></p>
  <p>
    <a href="https://github.com/Xxcool/juejin-csdn-extension/releases/latest">📥 下载安装包</a> ·
    <a href="https://artferry.vercel.app">🌐 项目网站</a> ·
    <a href="CHANGELOG.md">📜 更新日志</a>
  </p>
</div>

[![Release](https://img.shields.io/github/v/release/Xxcool/juejin-csdn-extension)](https://github.com/Xxcool/juejin-csdn-extension/releases)
[![CI](https://github.com/Xxcool/juejin-csdn-extension/actions/workflows/ci.yml/badge.svg)](https://github.com/Xxcool/juejin-csdn-extension/actions/workflows/ci.yml)
[![License](https://img.shields.io/github/license/Xxcool/juejin-csdn-extension)](LICENSE)

文章摆渡是一款 Chrome Manifest V3 浏览器扩展。它读取你主动选择的掘金文章或编辑器草稿，智能处理正文与图片，再安全着陆到目标平台的草稿箱。所有内容转换均在浏览器本地扩展中完成，直接与源站和目标平台通信，**不依赖任何托管账号密码的中转服务器**。

> ⚠️ **只同步草稿，不自动公开发布。** 同步完成后，请在目标平台检查排版、封面和内容，再自行决定发布。

---

## 🌟 1.0.0 支持什么

| 能力 | 🔴 CSDN | 🟢 微信公众号 |
| --- | --- | --- |
| 掘金新草稿、历史文章同步 | ✅ 支持 | ✅ 支持 |
| 正文格式 | 📝 Markdown 规范化与平台适配 | 🎨 微信专用 HTML 排版，内联关键样式 |
| 正文图片 | 🖼️ 自动转存；失败策略可配置 | 🖼️ 外链图片自动转存；转存失败终止本次同步 |
| 源文章封面 | 📌 按偏好设置同步 | 📐 自动上传、裁剪三套比例并绑定草稿 |
| 无封面文章 | ✅ 支持 | ✅ 正常保存草稿，不作失败处理 |
| 代码块 | 💻 保留 Markdown 代码块 | 💻 按微信“插入代码”结构逐行输出，保留空行和缩进 |
| 草稿更新 | 🔄 复用草稿映射，检查公开发布状态 | 🛡️ 按公众号账号隔离映射，写入前校验账号 |

> 💡 两个平台独立执行、独立记录结果。一次勾选多个平台，不代表所有平台都会同时成功。

### 📱 微信同步特别说明

- 🎨 **专属排版**：标题、段落、列表、引用、表格和图片按微信格式重新生成；外链保留为文末编号参考链接，不破坏阅读体验。
- 💻 **代码保真**：代码块使用微信专用 `section / pre / code` 结构，每行一个节点。语言标识来自 Markdown 围栏，支持 `js`、`ts` 等常见别名；未标注时不猜测。
- 📐 **封面裁剪**：源封面上传后自动生成 16:9、1:1、3:4 三套裁剪结果。**没有源封面也能同步**，若微信发布时要求封面，由你在微信后台补充即可。
- 🛡️ **防重更新**：更新失败不会自动盲目转为新建，避免产生重复草稿。保存结果不明确时，需要先核对草稿箱，再决定是否重试。
- 👤 **账号隔离**：任务与草稿映射深度绑定公众号身份。切换账号后，原账号任务不会使用新账号继续写入；未绑定账号的旧微信草稿记录不会被自动覆盖。

---

## 🚀 安装与升级

### 📦 下载 Release 安装包（推荐）

1. 📥 前往 [Releases 页面](https://github.com/Xxcool/juejin-csdn-extension/releases/latest) 下载最新版 `article-ferry-v1.0.0.zip` 并解压。
2. 🌐 打开 Chrome 浏览器，访问 `chrome://extensions/`，开启右上角的 **“开发者模式”**。
3. 📂 点击 **“加载已解压的扩展程序”**，选择解压出的目录。
4. 🔑 在同一 Chrome 配置文件中登录掘金和需要同步的目标平台。
5. 🔄 刷新已经打开的掘金页面，让注入脚本生效。

> 💡 **升级提示**：升级时，用新包内容覆盖原加载目录，再在扩展管理页点击“重新加载”🔄即可。保留原安装实例可保留其本地设置和记录；卸载扩展会清除本地数据。升级后同样需要刷新掘金页面。

### 💻 从源码运行

建议使用与 CI 一致的 Node.js 22 和 npm：

```bash
git clone https://github.com/Xxcool/juejin-csdn-extension.git
cd juejin-csdn-extension
npm ci
npm run check
npm test
npm run build
```

然后在 Chrome 扩展管理页加载仓库中的 `dist/` 目录。

---

## 🛠️ 如何同步

### 🎯 三个入口，同一套操作

- ✍️ **新文章编辑器**：写完并保存掘金草稿，点击顶栏右上角的“同步多平台”。
- 🎨 **创作者中心**：在自己的文章管理列表使用“同步多平台”。
- 👤 **个人主页**：在自己文章的操作菜单中使用“同步多平台”。

点击后，弹窗会自动检测登录状态：
1. 🔴 未登录的平台先点击 **“去登录 ↗”**，完成登录后返回刷新；
2. ✅ 勾选需要同步的目标平台（默认勾选受偏好设置控制，每次均可调整）；
3. 🚀 点击 **“确认同步”** 即可开始。

> 💡 刷新登录状态不会重新选中已被你取消的平台，操作完全尊重当前勾选。

### 📊 查看结果和再次同步

点击浏览器右上角扩展图标打开面板，“同步记录”支持查看成功、失败和进行中的任务：

- ✅ **已保存**：点击“草稿 ↗”前往目标平台检查。
- ⏳ **等待确认**：开启了“更新草稿前确认”时，需在记录中再次确认才会写入。
- ❌ **失败或需处理**：按诊断提示处理登录、图片或平台接口问题。
- ⚠️ **保存结果不确定**：先查看目标草稿箱。此类任务不参加批量重试，单条重试需二次确认，以免重复创建。

> 🛡️ 再次同步可能覆盖对应草稿的手工修改，建议开启“更新草稿前确认”。删除或清空本地同步记录不会删除目标平台草稿，并会妥善保留已有草稿映射。

---

## ⚙️ 偏好设置

- 🎯 **平台默认选择**：控制同步弹窗首次检测后的默认勾选，不表示自动发布。
- 📂 **CSDN 分类专栏**：自动读取已有专栏、设置默认分类，或按掘金标签匹配分类。
- 🖼️ **CSDN 内容选项**：封面、摘要、首发来源声明和图片转存失败策略。
- 🛡️ **更新草稿前确认**：已关联草稿的再次同步先进入待确认状态，防止误覆盖。

---

## 💡 使用边界与常见问题 (FAQ)

**❓ 同步后还需要重新上传图片吗？**
> 正常情况下不需要：扩展会自动转存正文图片和已有源封面。若图片源站防盗链拒绝下载、格式不被目标平台支持等情况仍可能转存失败，请查看诊断信息。微信正文图转存失败时会安全终止，绝不会把缺少图片的残缺文章当成成功。

**❓ 代码块会与掘金完全相同吗？**
> 微信不会复制掘金的 CSS 样式。微信代码块按微信公众平台专用的编辑器结构输出，完整保留代码行与缩进；颜色、高亮及手机端排版以微信官方编辑器渲染为准。

**❓ 为什么需要保持平台登录？**
> 扩展直接复用浏览器自身的官方登录会话，不代替扫码登录，也不绕过验证码或平台风控机制。登录状态失效时前往对应平台重新登录即可。

**❓ 更新微信草稿失败会新建一篇吗？**
> 不会。扩展已修复异常处理逻辑，不会把偶发网络抖动或接口报错当成“草稿不存在”，有效防止微信草稿箱产生多篇重复垃圾草稿。

**❓ 扩展重新加载后没有出现同步按钮？**
> 请刷新掘金页面，并确认正在自己的编辑器或文章管理入口操作。仅重新加载扩展不会自动替换已打开标签页里的旧脚本。

---

## 🔒 权限与隐私

- 🛡️ **零凭证托管**：不申请 `cookies` 敏感权限，不保存账号密码，绝不将登录凭据上传至任何第三方服务器。
- 💻 **本地数据安全**：本地存储仅包含设置、任务元信息、账号标识和草稿映射，历史记录不持久化全文 Markdown。
- ⚠️ **安全提醒**：微信草稿链接可能包含临时页面 token，请勿公开分享未经脱敏的存储导出、日志或链接。

| 🔑 权限或访问范围 | 📝 用途 |
| --- | --- |
| `storage` | 保存偏好设置、任务状态与草稿映射 |
| `notifications` | 后台长耗时同步结果系统通知 |
| `alarms` | 定期检查平台兼容性状态 |
| `declarativeNetRequestWithHostAccess` | 为已声明的平台接口补充必要请求头 |
| 掘金及声明的图片 CDN | 注入同步入口、读取源草稿和下载图片 |
| CSDN API 与图片存储服务 | 检查登录、读取分类、上传图片和保存草稿 |
| 微信公众平台 | 检查登录、上传/裁剪图片和保存草稿 |

完整说明详见 [隐私政策](PRIVACY.md)、[安全说明](SECURITY.md) 和 [manifest.json](manifest.json)。

---

## 🧪 开发与验证

项目基于 TypeScript、esbuild、Vitest 和 Marked 构建：

| ⚡ 命令 | 🎯 用途 |
| --- | --- |
| `npm run check` | TypeScript 类型检查 |
| `npm test` | 自动化回归测试（84 项全覆盖） |
| `npm run build` | 本地构建至 `dist/`，包含 source map |
| `npm run dev` | 监听源码变动并实时重构 |
| `npm run build:release` | 生产环境无 source map 纯净构建 |
| `npm run release:zip` | 严格检查、跑测、生产构建并打包 ZIP |
| `npm run release:notes` | 从 CHANGELOG 智能提取版本说明 |

1.0.0 发布前已通过 **84 项自动化测试**，覆盖微信会话、内容转换、图片封面、账号隔离、异常写入和弹窗状态等核心场景。

---

## 💬 反馈与贡献

欢迎通过 [GitHub Issues](https://github.com/Xxcool/juejin-csdn-extension/issues) 提交反馈或建议。请提供扩展版本、操作入口、目标平台、复现步骤及脱敏后的诊断日志；切勿提交 Cookie、token、私密文章正文等敏感信息。

本项目采用 [MIT License](LICENSE) 开源协议。第三方依赖说明见 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)。
