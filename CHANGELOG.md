# 更新日志

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-09-18

### ✨ 微信公众号完整同步

- 新增微信公众号网页登录态检测与账号展示，不申请 `cookies` 权限，不保存 Cookie 或上传票据；草稿链接可能包含微信页面 token。
- 新增微信专用 Markdown 编译器，按微信兼容约束重建标题、段落、列表、引用、代码块、表格与图片排版，并内联关键样式。
- 正文图片全部转存到微信素材库；源文章封面上传后自动生成 16:9、1:1、3:4 三套裁剪结果并绑定草稿。
- 掘金编辑器、创作者中心和个人文章列表统一使用“同步多平台”按钮，弹窗勾选目标平台后同步。
- 微信和 CSDN 使用独立任务、失败重试、进度、通知与草稿链接，正式版本号升级为 1.0.0。

### 🛠️ Review 修复

- 微信任务与草稿映射绑定公众号账号，开始和最终写入前校验账号；未绑定账号的旧草稿不自动覆盖。
- 更新失败不再自动新建；保存结果不确定时暂停，排除批量重试，单条重试需先确认。
- 保留代码中的 HTML 示例；外链以编号参考链接保留，图片替换仅修改图片地址。
- 代码块按微信“插入代码”结构逐行输出，保留空行、缩进与特殊字符，语言使用 Markdown 标注。
- 修复同步记录平台和状态标签挤压换行；无源封面同步不再显示警告。
- 弹窗区分已保存、等待确认与进行中；刷新不覆盖用户选择，提交期间锁定操作。
- 草稿封面与摘要缓存按草稿隔离，清空字段生效；无源封面文章允许同步草稿，发布所需封面由用户在微信后台补充。

### 🧪 验证

- 84 项自动化测试通过，覆盖微信会话、内容转换、图片封面、账号隔离、异常写入和弹窗状态。
- 维护者确认本地功能测试通过；整体更新 README，明确安装升级、同步流程、平台差异和隐私边界。

## [0.6.1] - 2026-09-17

### ✨ 体验与品牌

- 全量统一「文章摆渡」官方品牌标识：采用工业级 1024x1024 超采样与 Lanczos 抗锯齿渲染，将 Chrome 扩展管理页大图标、浏览器工具栏图标、视网膜 SVG 及 Favicon 全面升级为「一叶轻舟破浪 ⛵」专属舰徽，彻底淘汰旧版双文档复制图标。
- 接入全新独立官方展示与文档站点：弹窗「关于」卡片、README 与项目主页全面对齐极简生产域名 [artferry.vercel.app](https://artferry.vercel.app)。

### 🛠️ 工程与架构

- 文档站点与插件工程彻底解耦：官方站点独立上线至专用仓库 [Xxcool/artferry](https://github.com/Xxcool/artferry)，主仓库取消 `docs/` 索引跟踪并通过 `.gitignore` 永久忽略本地设计规划草稿。
- 优化 README 图标引用为相对路径，根除 GitHub Camo CDN 代理对 raw 图片历史缓存陈旧的问题。

## [0.6.0] - 2026-09-17

### ✨ 新增

- 新增 CSDN 分类专栏动态拉取：设置页默认分类输入框支持从当前登录账号实时拉取个人专栏列表（datalist 下拉建议），无需再手动翻查分类名。
- 新增自动摘要生成：优先使用掘金作者手填摘要（`brief`），缺失时从正文自动提取前 100 字纯文本（剥离代码块、图片、链接与行内标记），写入 CSDN 编辑器摘要字段；偏好设置可开关。
- 新增掘金首发声明注入：同步时在正文末尾追加「本文首发于掘金」引用声明（尊重平台原创规则，不占用转载 `original_link` 字段）；偏好设置可开关。
- 新增长耗时任务 Chrome 原生系统通知：任务耗时 ≥ 10 秒或外链图片 ≥ 3 张时播报结果，点击通知直达 CSDN 草稿页；短任务保持安静不打扰。
- 新增同步前 Dry-run 预演：任务卡片新增「预演」按钮，静态分析标题长度、正文字数、分类命中、外链图片数、掘金容器与代码围栏数量并弹窗展示风险清单，全程不转存图片、不写入草稿。
- 新增掘金图片无水印直链提取：转存前剔除掘金 CDN 的 `~tplv-` 模板段与 `x-oss-process` 水印参数获取原始图，直链失效时自动回退原链接。
- 新增轻量远程健康检查：每 12 小时后台拉取仓库发布的 `health.json` 状态文件，任一平台接口 broken 时在弹窗顶部显示警示条，可一键跳转状态页；检查失败静默降级，绝不阻断同步。

### 🛠️ 修复

- 修复 CSDN 侧草稿被删除后任务永久卡死：更新模式遇到 400「该文章不存在」时自动降级为新建草稿，新文章 ID 自动覆盖旧映射，实现自愈；降级时复用已转存图片，不重复下载上传。
- 修复成功系统通知永不触发：任务保存成功后构建通知对象时显式标记 saved 状态（patchTask 只写存储不改局部变量，原实现状态恒为入参旧值）。
- 修复远程健康检查定时器失效：MV3 Service Worker 每次唤醒重跑顶层代码导致同名 alarm 计时不断重置，改为先检查已存在再创建；同时新增 12 小时缓存防抖，缓存有效期内不再重复发起网络请求。
- 修复同步预演风险清单常黄：首发声明注入属功能告知而非风险，拆分至独立的中性提示区，配置完备的合规文章可正常展示绿色就绪态。
- 修复快速失败任务被静默吞掉通知：失败/需用户介入的任务豁免 10 秒时长门槛，避免用户切走后误以为同步成功。
- 修复任务数量为 0 时同步记录徽章仍显示空胶囊：0 时完全隐藏。
- CSDN 分类接口字段做 categories 拼写兼容防守（实测下发为拼写错误的 categorys）。
- 修复弹窗 CSS 三处显示问题：`hidden` 属性被 `display:flex` 覆盖导致弹窗与警示条常显、预演报告长标题单行截断、弹窗内容超高时滚动失效（flex 子项 `min-height` 陷阱）。
- 通知点击兜底打开 popup.html 时在独立标签页居中展示，不再缩在左上角。

### 🧪 测试

- 新增 12 项 v0.6.0 专项测试：摘要提取、首发声明构建、水印参数剔除、分类接口解析（含拼写兼容）、预演报告（含就绪态拆分）、系统通知触发阈值（含快速失败豁免）、健康状态解析与降级、健康检查缓存防抖、设置新字段规范化、保存请求摘要契约、删除草稿降级新建，全量 41 项测试通过。

## [0.5.3] - 2026-09-17

### ✨ 新增

- 新增二级 Tab（筛选标签）集成式微胶囊轨道，支持选中态日光白药丸与动态数量角标。
- 新增任务卡片「草稿 ↗」蓝色高光实体渐变按钮（`--brand-gradient` + 日光内微光）与 1px 精细微边框状态徽章（`✓ 已抵达草稿箱`）。
- 新增任务卡片 10px 优雅圆角与深空渐变缩略图底图，核心度量数据结构化加粗展示。
- 新增进行中任务进度条动态微光航线流光动效（`lightShimmer`）。

### 🛠️ 修复

- 修复双 Tab 滑块位移多出 2px 偏移的问题，彻底解决「偏好设置」激活时右侧外边距偏小问题，实现两端 1:1 绝对物理对称（两端均标准 3px 边距）。
- 修复「偏好设置」与「同步记录」外边距不一致问题，左右外边距严格对齐为 14px，移除破坏对称的 `scrollbar-gutter` 与 9px 偏差。
- 修复偏好设置顶部外边距过小问题，顶部缓冲优化至 12px，避免小标题紧贴顶部分段导航。
- 修复偏好设置分组卡片圆角偏小问题，统一升级至 14px 圆角与日光顶高光（`--hairline-top`）。
- 修复 CSDN 在线绿点呼吸光晕扩散范围过大问题，约束波纹半径与边框，动效更加轻盈克制。

## [0.5.2] - 2026-09-17

### ✨ 新增

- 新增「旗舰工艺版」现代设计系统：专属一叶轻舟破浪摆渡舰徽、CSDN 在线状态呼吸光晕、物理弹簧分段滑块、380×470 紧凑视口与自适应深色模式。
- 新增「渡口夜灯」空状态温感插画与清晰实用的 3 步实操指南。

### 🛠️ 修复

- 修复进行中任务实时进度文案丢失问题：任务运行态完整展示 `task.progress.message`（例如「正在转存图片 3/10」）与实时百分比，未定总量阶段引入动态穿梭流光动画。
- 修复顶栏版本号硬编码导致的漂移问题：全局通过 `chrome.runtime.getManifest()` 动态注入版本号，确保顶栏与偏好设置页完全同步。
- 修复 GitHub Actions 发布工作流：发布 Draft 与 Release 标记对齐，确保发布产物自动同步最新 Release。

## [0.5.1] - 2026-09-16

### 修复 (Fixes)

- 修复新发文章发布面板标签提取失效：主策略经 MAIN 世界 API 钩子缓存 `article_draft` 保存/详情响应中的标签，DOM 兜底解析发布面板选中标签，恢复「掘金标签 → CSDN 分类」映射精准度。
- 修复掘金深色模式下注入控件样式失效：实证修正为 `body[data-theme="dark"]` 单一选择器，剔除无效的双猜测分支。

### 可靠性与安全防御 (Reliability & Safety)

- 新增 CSDN 线上已发布博文状态检测：写入草稿前查询文章发布状态，已发布文章物理阻断覆盖同步；状态接口异常时放行（fail-open），不阻断正常草稿同步。
- 任务历史 Storage 瘦身：持久化剥离全量 Markdown 正文并在升级时自动迁移存量数据，根除 `storage.local` 10MB 配额爆仓风险；重试时经 `draft_id` 直查或 `article_id` 反查双路径回填正文。
- 后台任务执行引入全局并发闸门（Semaphore，上限 2），批量重试不再突发并发，杜绝触发平台 429 限流。
- DNR 网络拦截规则收窄至 `list_by_user`、`article_draft/detail` 与 `getArticle` 指定接口路径，不再覆盖整个掘金 API 域。
- 引入结构化错误体系（`SyncError`：错误类别 + 阶段），图片凭证 5xx 等网络异常不再被误判为内容异常，失败归因与修复建议精准度全面提升。

### 内容清洗与优化 (Content)

- 新增掘金容器语法清洗：`:::tips` 等内置容器自动转换为标准 Markdown 引用块，代码围栏内内容严格保护，未知容器类型保留原名称继续转换。

### 测试 (Tests)

- 新增 7 项底座加固专项测试：并发闸门峰值、存储瘦身与存量迁移、草稿双路径反查、已发布状态检测分支、结构化错误分类优先级、容器清洗与 DNR 规则收窄，全量 30 项测试通过。

## [0.5.0] - 2026-09-16

### 界面与体验 (UI / UX)

- 弹窗重构为一体化双 Tab 仪表盘布局，打开即见同步任务与草稿链接，移除原有三级深度菜单。
- 顶栏新增 CSDN 状态胶囊，直观展示账号与在线状态，支持一键点击刷新与跳转登录。
- 建立规范化设计系统与 Design Tokens，彻底清除 9px/10px 劣质字号，全面提升文字对比度与可读性。
- 全套支持自适应深色模式（Dark Mode），夜间写作视觉柔和不刺眼。
- 任务卡片诊断信息支持一键复制脱敏内容到剪贴板，空状态增加友好使用指引与掘金文章管理直达入口。
- 掘金编辑器注入控件拟态 Semi Design 官方规范，文案精简为“同步到 CSDN 草稿”，支持深色主题。
- 任务筛选状态下动态显示消歧条目数（例如 `2/8 篇`），避免总数与过滤视图产生歧义。

### 无障碍与交互细节 (Accessibility)

- 设置开关（Switch）全面改造为无障碍友好架构（采用 `visually-hidden` 配合 `:focus-visible` 焦点环），支持键盘 Tab 导航与屏幕阅读器无障碍操作。
- 双 Tab 补齐标准 WAI-ARIA 语义（`role="tablist"` / `role="tab"` / `role="tabpanel"`），支持键盘左右箭头（`←` / `→`）平滑轮转与自动聚焦。
- 扩展版本号改为基于 `manifest.json` 动态注入渲染，彻底杜绝手动写死导致的潜在版本漂移。

### 优化与重构 (Refactoring)

- 彻底移除历史遗留补丁 `src/popup-fixes.css`，合并所有样式变量至统一的设计令牌系统中。
- 清理掘金页面注入控件中遗留的废弃 `.jc-sync-published` 样式。

### 文档与规划 (Documentation)

- 建立产品全景主规划单一事实来源（SSOT `docs/product-roadmap.md`），明确 P0 致命缺陷防御体系与演进路线。
- 归档历史 UI/UX 专项重构方案（`docs/ui-ux-refactoring-plan.md`），统一规划权威入口。
- 修复项目 README 中的失效文档链接。

### 工程与发布 (Engineering)

- 建立 GitHub Release Notes 自动化提取与美化流程，发版时自动基于 CHANGELOG 提取并分类渲染高颜值 Emoji。
- 新增 `npm run release:notes` 脚本，支持在本地随时预览发布日志。


## [0.4.1] - 2026-09-01

### 修复

- 新建文章勾选同步后，在点击“确定并发布”时立即创建 CSDN 同步任务，不再等待掘金审核或页面跳转。
- 历史文章同步同时使用正式文章 ID 和 `draft_id` 查找草稿映射，避免同一文章生成重复 CSDN 草稿。
- 历史同步更新已有草稿后显示明确的更新结果。
- 按 CSDN 的 5～100 字标题规则在请求前校验，并将“标题过短”归类为内容问题。
- 内容校验失败时仍保留同步历史记录，避免发布跳转后无法查看失败原因。

### 测试

- 增加新建与历史文章身份匹配、双身份草稿映射、标题校验和消息契约测试，共 22 项。

## [0.4.0] - 2026-09-01

### 新增

- 支持默认 CSDN 分类，以及“掘金标签 → CSDN 分类”的名称映射。
- 增加封面同步、图片失败处理和更新已有草稿前确认设置。
- 同步历史支持状态筛选、删除单条本地记录和批量重试失败任务。

### 改进

- 旧版设置自动补齐新字段，升级后保留原有发布联动行为。
- 分类映射按标签名称匹配，多个分类去重后写入 CSDN 草稿。
- 图片失败可继续保留原链接，也可按用户设置终止本次同步。

### 测试

- 增加设置迁移、分类映射、任务操作和图片失败策略回归测试，共 19 项。

## [0.3.0] - 2026-09-01

### 新增

- 同步失败按登录、网络、限流、平台接口变化、内容和任务中断分类，并记录发生阶段和处理建议。
- 同步历史展示处理耗时及图片转存成功、失败数量。
- 增加发布前人工验收清单，覆盖首次同步、草稿更新、历史文章、登录续接、图片失败和任务恢复。

### 改进

- 对错误信息中的令牌、签名等敏感参数进行脱敏。
- 后台仅处理已声明的扩展消息，未知消息返回明确错误。
- 核心回归测试由 12 项增加至 15 项。

## [0.2.1] - 2026-09-01

### 修复

- 修复跨平台同步中的草稿身份确认、历史任务恢复和异常状态展示。
- 补充分页读取历史文章及 CSDN 鉴权异常的回归覆盖。

### 文档

- 更新功能、可靠性、隐私和第三方依赖说明。

## [0.2.0] - 2026-09-01

### 新增

- 图片转存进度、三路限流并发和失败退避重试。
- Service Worker 或浏览器重启后的未完成任务恢复。
- Vitest 核心回归测试，并在 CI 与发布构建中强制执行。

### 修复

- 同一篇掘金文章再次同步时更新已有 CSDN 草稿，减少重复草稿。
- 个别正文图片转存失败时保留原链接继续保存，封面失败时忽略封面并显示警告。
- 同步历史实时刷新任务进度和部分失败警告。

## [0.1.1] - 2026-09-01

### 修复

- 修复掘金私有 CDN 图片因缺少登录会话导致的 403 下载失败。
- 补充掘金子域名访问权限与网络请求规则。
- 同一文章的同步历史合并为最新记录。
- 修复历史卡片被压缩裁切的问题，并增加可见的纵向滚动条。

## [0.1.0] - 2026-08-31


### 新增

- 掘金新文章发布成功后同步到 CSDN 草稿箱。
- 本人历史文章列表“同步到 CSDN”入口。
- CSDN 登录检测和登录后的任务续接。
- Markdown 主题元数据清理、GFM 代码块处理和图片转存。
- 本地同步历史、失败原因、草稿入口和重新同步。

### 安全

- 将请求头规则限制到实际博客编辑及图片上传接口。
- 排除由掘金和 CSDN 页面自身发起的请求，避免影响站点原有网络行为。
